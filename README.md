# Installing
Requires lazy nvim

Run `intallinit.sh` to install the init file to `~/.config/nvim/init.lua`
# Usage
Custom keybindings (leader key is <space>):
- `"e"` Open file explorer
- `"t"` Open telescope
- `"ni"` Open init.lua 
